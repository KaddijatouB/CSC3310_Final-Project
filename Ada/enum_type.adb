with Ada.Text_IO; 
use Ada.Text_IO;

procedure Main is
-- Declay an enum for family member names
    type MyFamily is
    (Jerome,
    Kay,
    Jabin,
    Jacobi,
    Michaella,
    Donald);
    
    begin

end Main;